extends layouts/default

block vars
	- var page = 'system'
	- var title = 'Looks like something has gone wrong…'
	- var urlFriendlyShareText = encodeURI(shareText);

block content
	.page-content-error
		h2.page-content-error-title ☹
